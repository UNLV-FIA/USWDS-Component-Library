## Using Demo Application

### Steps
1. Enter the `ngx-uswds-component-lib` project folder and run `npm link`.
2. Enter the `uswds-component-library-demo-app` project folder and run `npm link <library name>` 